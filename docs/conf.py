# conf.py

# Path to static files (where custom.css is located)
html_static_path = ['static']

# Add the custom CSS file to the documentation
html_css_files = [
    'custom.css',  # This file will be loaded in the final HTML output
]
